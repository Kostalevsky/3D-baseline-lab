# **Методология обработки 3D-объектов**

---

## **ISO-ориентированный метод ортографических проекций**

В качестве первого базового подхода для подготовки визуального представления 3D-объекта был рассмотрен ISO-ориентированный метод ортографических проекций. В отличие от адаптивных методов, которые выбирают наиболее информативные ракурсы на основе геометрии объекта или с помощью модели, данный подход использует заранее заданную инженерную схему представления формы. Объект переводится в набор стандартных видов: спереди, сверху, снизу, слева, справа и сзади. Эти виды формируют технический коллаж, который может быть использован как вход для последующей работы мультимодальной языковой модели.

Метод основан на логике ортографического проецирования, описанной в стандарте ISO 5456-2. В таком представлении 3D-объект отображается на плоскости без перспективных искажений, поэтому каждый вид сохраняет геометрические пропорции соответствующей стороны объекта. В зависимости от выбранной системы расположения видов могут использоваться first-angle projection или third-angle projection. В third-angle projection верхний вид располагается над главным видом, нижний — под ним, левый — слева, правый — справа. В first-angle projection расположение противоположное: верхний вид находится под главным видом, нижний — над ним, левый вид — справа, а правый — слева.

Для задачи генерации коллажа этот подход можно рассматривать как воспроизводимый baseline. Он не требует обучения модели, не зависит от семантики объекта и может быть применён к разным форматам 3D-данных, например OBJ, STL, PLY или GLB. На вход программа получает 3D-модель, нормализует её по центру и масштабу, после чего рендерит шесть ортографических проекций с фиксированных направлений камеры. Далее изображения размещаются в коллаже согласно выбранной схеме: first-angle или third-angle. Такой коллаж позволяет представить объект в форме, близкой к техническому чертежу, и делает структуру объекта более понятной для дальнейшего анализа.

Преимущество ISO-подхода заключается в его простоте, стандартизированности и интерпретируемости. Он хорошо подходит для инженерных и CAD-объектов, где важны форма, пропорции, симметрия, отверстия, выступы и взаимное расположение элементов. Кроме того, этот метод удобен как контрольная точка для сравнения с более сложными подходами: алгоритмическим выбором информативных ракурсов, multi-view методами и LLM/VLM-based view selection.

Ограничение метода состоит в том, что он не выбирает ракурсы адаптивно. Для некоторых объектов часть стандартных видов может быть малоинформативной или дублировать уже видимые признаки. Кроме того, ортографические проекции не всегда хорошо передают объёмную структуру органических или сложных объектов. Поэтому ISO-подход целесообразно использовать как базовый технический способ генерации коллажа, а затем сравнивать его с методами, которые автоматически оценивают информативность ракурсов.

## **Cap3D-style метод многоракурсного представления объекта**

В качестве второго подхода был рассмотрен Cap3D-style метод, основанный на представлении 3D-объекта через набор изображений, полученных с разных ракурсов. В отличие от ISO-ориентированного подхода, где используются фиксированные ортографические виды, данный метод стремится показать объект более естественно для мультимодальной модели: не как технический чертёж, а как серию визуальных наблюдений вокруг объекта.

Оригинальный подход Cap3D направлен на генерацию текстовых описаний 3D-объектов. В общем виде его логика строится следующим образом: 3D-объект рендерится с нескольких ракурсов, затем для полученных изображений генерируются текстовые описания с помощью image captioning моделей, после чего результаты фильтруются, сопоставляются и объединяются в итоговое описание объекта при помощи языковой модели. Таким образом, Cap3D использует 2D-представления 3D-модели как промежуточный слой между геометрией объекта и языковым описанием.

В рамках текущей реализации был воспроизведён не полный Cap3D pipeline, а его визуальный preprocessing-этап. Программа получает 3D-модель, нормализует её по центру и масштабу, после чего рендерит восемь изображений объекта с равномерно распределённых ракурсов вокруг вертикальной оси. Камера движется по окружности вокруг модели, сохраняя одинаковую высоту и расстояние до центра объекта. Полученные изображения затем объединяются в коллаж, который может быть использован как вход для дальнейшей обработки мультимодальной моделью.

Такой подход можно описать как:
```
3D object
    ↓
mesh normalization
    ↓
render 8 views around the object
    ↓
assemble multi-view collage
    ↓
save collage + metadata
```

В отличие от ISO-подхода, Cap3D-style baseline использует перспективную камеру и диагональные ракурсы. Это позволяет лучше передавать объёмную форму объекта, взаимное расположение частей и общий внешний вид. Такой формат ближе к тому, как объект воспринимается человеком или visual-language моделью при просмотре набора изображений.

Преимущество Cap3D-style подхода заключается в том, что он лучше подходит для визуального понимания формы в задачах, где важны не только отдельные стороны объекта, но и его общий силуэт, объём и характерные детали. Он особенно полезен для объектов, которые трудно описать только через front/top/side views. Кроме того, такой baseline хорошо связан с дальнейшей LLM/VLM-логикой: на следующем этапе к каждому виду можно добавить автоматическое captioning-описание, а затем объединить эти описания в итоговое описание 3D-объекта.

Ограничение текущей реализации состоит в том, что она пока не включает captioning-модуль и не выполняет полноценное текстовое описание объекта. Реализован только этап генерации визуального представления: рендеринг нескольких видов и сборка коллажа. Кроме того, ракурсы выбираются фиксированно и равномерно, без оценки их информативности. Поэтому данный подход следует рассматривать как промежуточный baseline между строгим ISO-представлением и более сложными методами, где ракурсы выбираются адаптивно или с помощью мультимодальной модели.

## **Алгоритмический метод выбора информативных ракурсов**

В качестве третьего подхода был рассмотрен алгоритмический метод выбора информативных ракурсов. В отличие от ISO-подхода, где набор видов задаётся инженерным стандартом, и Cap3D-style подхода, где используется фиксированное количество равномерно распределённых ракурсов, данный метод пытается автоматически определить, какие изображения объекта являются наиболее полезными для дальнейшего анализа.

Общая логика метода состоит в том, что программа сначала рендерит расширенный набор candidate views. В текущей реализации используются 24 кандидатных ракурса: восемь углов азимута вокруг объекта и три уровня высоты камеры. После этого каждый rendered view оценивается с помощью простых визуальных признаков, которые можно посчитать без обучения модели.

Для оценки информативности используются следующие признаки:

```text
1. Silhouette area — доля изображения, занятая объектом.
2. Bounding box fill ratio — насколько плотно объект заполняет свой 2D bounding box.
3. Edge / contour complexity — количество контурных переходов в изображении.
4. View diversity — отличие выбранного ракурса от уже отобранных видов.
```

Silhouette area помогает отсеивать ракурсы, где объект занимает слишком мало места в кадре. Bounding box fill ratio показывает, насколько компактно объект представлен в изображении. Edge / contour complexity полезен для объектов с отверстиями, вырезами и сложной внешней границей: чем больше контуров видно, тем потенциально информативнее вид. View diversity добавляется на этапе выбора top views, чтобы итоговый коллаж не состоял из почти одинаковых ракурсов.

В текущей реализации итоговый score считается как взвешенная сумма:

```text
final_score =
    0.45 * area_score
  + 0.35 * edge_score
  + 0.20 * bbox_fill_score
```

После расчёта score программа выбирает восемь лучших ракурсов. Выбор выполняется greedy-алгоритмом: первый вид выбирается по максимальному score, а следующие получают дополнительный бонус за угловое отличие от уже выбранных ракурсов. Это позволяет сохранить баланс между информативностью и разнообразием итогового коллажа.

Такой подход можно описать как:

```text
3D object
    ↓
mesh normalization
    ↓
render 24 candidate views
    ↓
calculate visual score for each view
    ↓
select top 8 diverse views
    ↓
assemble collage
    ↓
save collage + metadata
```

Преимущество алгоритмического подхода заключается в том, что он не требует обучения модели и остаётся полностью интерпретируемым. В metadata сохраняются все оценки, поэтому можно понять, почему конкретные ракурсы были выбраны. Это делает метод удобным baseline для сравнения с LLM/VLM-based view selection, где выбор ракурсов может быть менее прозрачным.

На плоских инженерных деталях такой метод особенно полезен: чисто боковые виды часто оказываются малоинформативными, потому что объект выглядит как тонкая полоска. Алгоритмический baseline, напротив, выбирает верхние и диагональные ракурсы, где видны отверстия, силуэт, толщина и пространственная форма детали.

Ограничение метода состоит в том, что он оценивает только визуальные признаки rendered image и не понимает семантику объекта. Например, ракурс может быть важен с инженерной точки зрения, даже если его silhouette area или contour complexity невысоки. Поэтому данный подход следует рассматривать как объяснимый geometric/visual baseline, а не как полноценную замену семантическому выбору ракурсов с помощью VLM.

## **Isomap View Selection метод многоракурсного отбора**

В качестве четвёртого подхода был рассмотрен Isomap View Selection метод. Он развивает идею многоракурсного представления объекта, но в отличие от Cap3D-style baseline не ограничивается фиксированными восемью изображениями. Метод сначала формирует расширенный набор из 28 rendered views, после чего выполняет отбор наиболее информативных и разнообразных ракурсов.

Общая логика подхода состоит в том, что 3D-объект сначала рендерится с разных направлений камеры. В текущей реализации используется 28 ракурсов:

```text
Elevation 15°: 8 azimuth views
Elevation 35°: 8 azimuth views
Elevation 60°: 8 azimuth views
Elevation 80°: 4 high-angle views

Total: 28 views
```

Таким образом, метод получает набор изображений, в котором есть низкие боковые ракурсы, средние диагональные виды, верхне-диагональные виды и почти top-view изображения. Это позволяет описать объект более полно, чем при использовании только одного фиксированного кольца ракурсов.

После генерации 28 изображений для каждого вида извлекается feature vector. Он включает уменьшенное grayscale-представление изображения и несколько простых визуальных признаков: площадь объекта в кадре, плотность заполнения bounding box и сложность контуров. Эти признаки позволяют представить каждый rendered view как числовой вектор.

Далее используется Isomap. Isomap строит двумерное embedding-пространство, в котором похожие ракурсы располагаются ближе друг к другу, а визуально отличающиеся ракурсы — дальше. Это позволяет выбирать не просто изображения с максимальной отдельной оценкой, а набор видов, который покрывает разные визуальные состояния объекта.

Общий пайплайн метода можно описать так:

```text
3D object
    ↓
mesh normalization
    ↓
render 28 views
    ↓
extract image features
    ↓
build Isomap embedding
    ↓
quality-aware view selection
    ↓
assemble selected collages
    ↓
save images + metadata
```

Изначально выбор ракурсов может быть выполнен через farthest point sampling в Isomap-пространстве: метод выбирает точки, которые находятся далеко друг от друга, чтобы итоговый набор видов был разнообразным. Однако для плоских инженерных деталей одного разнообразия недостаточно: низкие боковые изображения могут быть сильно отличаться от остальных в embedding, но при этом быть малоинформативными визуально, потому что объект выглядит как тонкая полоска.

Поэтому в финальной версии selection стал quality-aware. Для каждого вида рассчитывается `quality_score`:

```text
quality_score =
    0.45 * area_score
  + 0.35 * edge_score
  + 0.20 * bbox_score
```

После этого применяется минимальный порог качества:

```text
quality_threshold = 0.45
```

Итоговый selection score учитывает и разнообразие в Isomap-пространстве, и визуальную информативность изображения:

```text
selection_score =
    0.60 * isomap_diversity
  + 0.40 * visual_quality
```

На выходе метод сохраняет несколько изображений:

```text
all_views_collage.png — все 28 rendered views
collage_3.png — 3 выбранных вида
collage_4.png — 4 выбранных вида
collage_6.png — 6 выбранных видов
embedding.png — визуализация Isomap-пространства
metadata.json — параметры запуска, признаки, embedding и выбранные views
```

Преимущество Isomap View Selection метода заключается в том, что он сочетает два принципа: визуальную информативность отдельных изображений и разнообразие всего набора. В отличие от простого top-k scoring, метод стремится выбрать не несколько похожих “лучших” ракурсов, а такой набор изображений, который покрывает разные стороны объекта.

На тестовой плоской инженерной детали финальная версия метода выбрала преимущественно ракурсы с elevation 60° и 80°, а также один информативный ракурс с elevation 35°. Это показывает, что quality-aware selection отсекает слабые боковые виды и выбирает изображения, на которых лучше видны отверстия, силуэт, толщина и общая форма объекта.

Ограничение метода состоит в том, что он всё ещё использует image-based признаки и не понимает семантику объекта. Он может определить, какие виды визуально различаются и какие изображения содержат больше контуров или площади объекта, но не понимает инженерное назначение детали. Поэтому Isomap View Selection следует рассматривать как более сложный интерпретируемый baseline перед переходом к VLM-based view selection.

## **VLM-guided итеративный метод выбора информативных ракурсов**

В качестве пятого подхода был рассмотрен VLM-guided итеративный метод выбора информативных ракурсов. В отличие от предыдущих baseline-методов, где выбор ракурсов выполняется по фиксированному правилу, визуальному score или embedding-пространству, данный подход использует мультимодальную модель для анализа уже собранного коллажа и последующего улучшения набора видов.

Метод основан на идее, что выбор информативных ракурсов можно рассматривать как итеративную задачу. Сначала программа формирует расширенный набор candidate views, затем создаёт первичный коллаж из наиболее качественных изображений, после чего мультимодальная модель оценивает, насколько текущий набор видов хорошо раскрывает объект. Если в коллаже есть слабые, избыточные или недостаточно информативные виды, модель предлагает заменить их на другие ракурсы из общего набора кандидатов.

В текущей реализации метод использует тот же набор из 28 rendered views, что и Isomap View Selection baseline:

```text

Elevation 15°: 8 azimuth views

Elevation 35°: 8 azimuth views

Elevation 60°: 8 azimuth views

Elevation 80°: 4 high-angle views

Total: 28 views
```

Первичный набор видов формируется детерминированно: программа выбирает изображения с наибольшим visual quality score. После этого создаётся iteration_0_collage.png, который передаётся мультимодальной модели вместе с полным коллажем всех 28 кандидатных ракурсов. Таким образом, модель видит и текущий выбранный набор, и полный список возможных альтернатив.

Общий пайплайн метода можно описать так:

```text
3D object
    ↓
mesh normalization
    ↓
render 28 candidate views
    ↓
create initial top-quality selection
    ↓
send all views + current collage to VLM
    ↓
receive JSON decision
    ↓
update selected views
    ↓
repeat for N iterations
    ↓
save final collage + metadata
```

На каждой итерации VLM получает несколько типов информации. Во-первых, она видит полный визуальный набор доступных ракурсов. Во-вторых, она видит текущий выбранный коллаж. В-третьих, в prompt передаётся metadata по каждому виду: индекс, azimuth, elevation, quality score, area ratio, edge ratio и bounding box fill score. Это ограничивает задачу: модель не генерирует новые ракурсы, а выбирает только из заранее подготовленного списка candidate views.

Ответ модели сохраняется в структурированном JSON-формате:

```json
{
  "previous_selected_view_indices": [16, 23, 17, 22, 8, 18],
  "selected_view_indices": [16, 23, 17, 22, 8, 20],
  "removed_view_indices": [18],
  "added_view_indices": [20],
  "reasoning": "The updated set provides a more informative and diverse view selection.",
  "visible_features": ["overall shape", "holes", "silhouette", "thickness"],
  "missing_or_weak_features": [],
  "confidence": 0.95
}
```

Такой формат важен для воспроизводимости и анализа результата. В отличие от обычного black-box вызова мультимодальной модели, программа сохраняет не только финальный коллаж, но и промежуточные решения модели: какие виды были удалены, какие добавлены и почему. Это позволяет анализировать поведение VLM и сравнивать его с алгоритмическими baseline-методами.

В реализованном эксперименте на тестовой плоской инженерной детали начальный набор был следующим:

```text
16 — AZ000 EL60
23 — AZ315 EL60
17 — AZ045 EL60
22 — AZ270 EL60
08 — AZ000 EL35
18 — AZ090 EL60
```

После первой итерации VLM предложила заменить один вид:

```text
removed: 18 — AZ090 EL60
added:   20 — AZ180 EL60
```

Финальный набор стал:

```text
16 — AZ000 EL60
23 — AZ315 EL60
17 — AZ045 EL60
22 — AZ270 EL60
08 — AZ000 EL35
20 — AZ180 EL60
```

На второй итерации модель не предложила новых изменений, так как посчитала набор достаточно сбалансированным. Это показывает, что метод может не только менять набор ради изменения, но и стабилизироваться, если текущий коллаж уже покрывает основные визуальные свойства объекта.

Преимущество VLM-guided подхода заключается в том, что он добавляет семантически ориентированный слой поверх геометрических и image-based признаков. Если Algorithmic и Isomap baseline оценивают изображения через площадь, контуры, bounding box и embedding, то VLM может рассматривать коллаж как визуальное представление объекта и оценивать, хватает ли ракурсов для понимания формы, отверстий, толщины, симметрии и пространственной структуры.

Ограничение метода состоит в зависимости от внешней мультимодальной модели. Результат может зависеть от выбранной модели, prompt, API-провайдера и параметров генерации. Кроме того, текстовое reasoning модели не всегда идеально точно: модель может правильно выбрать ракурс, но неточно описать его технические параметры. Поэтому данный метод следует рассматривать как экспериментальный VLM-based baseline, а не как полностью объективную замену алгоритмическим методам. Для контроля результата все решения модели сохраняются в metadata.json.

Таким образом, VLM-guided итеративный baseline завершает текущую линейку методов:

```text
fixed technical views
    ↓
fixed multi-view rendering
    ↓
algorithmic visual scoring
    ↓
manifold-based view selection
    ↓
VLM-guided iterative view selection
```

---

## Описание реализованной программы

Для проверки рассмотренных методов была начата разработка экспериментальной Python-системы, позволяющей загружать 3D-объект, выбирать baseline-метод обработки и получать на выходе визуальный коллаж с метаданными запуска. Основная идея программы заключается в том, чтобы разные методы подготовки 3D-объектов можно было запускать в одном формате и затем сравнивать их результаты между собой.

На текущем этапе система поддерживает пять baseline-методов:

```text
1. ISO Orthographic Baseline
2. Cap3D-style Views Baseline
3. Algorithmic Top Views Baseline
4. Isomap View Selection Baseline
5. VLM Iterative View Selection Baseline
```

Каждый baseline реализует собственную стратегию выбора и рендеринга ракурсов. ISO-подход использует стандартные ортографические проекции, Cap3D-style baseline создаёт фиксированные восемь перспективных видов вокруг объекта, Algorithmic Top Views baseline сначала генерирует расширенный набор кандидатных ракурсов, оценивает их по визуальным признакам и выбирает восемь наиболее информативных видов, а Isomap View Selection baseline рендерит 28 ракурсов, строит Isomap embedding и выбирает наборы из 3, 4 и 6 информативных и разнообразных видов.

```text
input 3D model / demo object
        ↓
load mesh
        ↓
normalize mesh
        ↓
run selected baseline
        ↓
render views
        ↓
assemble collage
        ↓
save output image
        ↓
save metadata JSON
```

Программа написана на Python и использует несколько основных библиотек. `trimesh` применяется для загрузки и базовой обработки 3D-моделей, `pyrender` — для рендеринга объекта в изображения, `Pillow` — для сборки финальных коллажей, `numpy` — для работы с координатами камеры, векторами и расчётом визуальных признаков, `scikit-learn` — для построения Isomap embedding, `openai` — для обращения к OpenAI-compatible API мультимодальных моделей, а `python-dotenv` — для загрузки локальных API-ключей из `.env`.

Важная особенность системы — единый формат входа и выхода. Независимо от выбранного baseline, программа принимает 3D-модель, нормализует её, запускает выбранный метод и сохраняет результат в виде `collage.png` и `metadata.json`. Для некоторых методов могут сохраняться дополнительные изображения: например, Isomap View Selection baseline сохраняет общий коллаж из 28 видов, отдельные коллажи выбранных ракурсов и визуализацию embedding-пространства. Это позволяет постепенно добавлять новые подходы и сравнивать их между собой в одной структуре проекта.

---

## Структура проекта

Текущая структура проекта выглядит следующим образом:

```text
3d-baseline-lab/
    README.md
    METHODS_DESCRIPTION.md
    requirements.txt
    pyproject.toml

    src/
        baseline_lab/
            __init__.py
            cli.py
            demo.py
            io.py

            baselines/
                __init__.py
                iso.py
                cap3d.py
                algorithmic.py
                isomap.py
                vlm_iterative.py

    examples/
        models/
            test_part.obj

    outputs/
        iso/
            test_part_iso_first/
                collage.png
                metadata.json

            test_part_iso_third/
                collage.png
                metadata.json

        cap3d/
            test_part_cap3d_views/
                collage.png
                metadata.json

        algorithmic/
            test_part_algorithmic_top8/
                collage.png
                metadata.json

        isomap/
            test_part_isomap/
                collage.png
                all_views_collage.png
                collage_3.png
                collage_4.png
                collage_6.png
                embedding.png
                metadata.json

        vlm_iterative/
            test_part_vlm_iterative/
                collage.png
                all_views_collage.png
                iteration_0_collage.png
                iteration_1_collage.png
                iteration_2_collage.png
                final_collage.png
                metadata.json
```

Файл cli.py отвечает за запуск программы из терминала. В нём описаны аргументы командной строки: путь к модели, выбор baseline, размер изображений, тип ISO-проекции, название запуска и папка для сохранения результатов. Через этот файл пользователь выбирает, какой метод обработки применить к 3D-объекту.

Файл io.py отвечает за загрузку и нормализацию 3D-модели. Он принимает путь к файлу, загружает объект через trimesh, объединяет несколько geometry-объектов в один mesh, если модель была загружена как сцена, и возвращает подготовленный объект для дальнейшей обработки.

Файл demo.py содержит встроенный тестовый объект. Он нужен для проверки программы без внешнего 3D-файла. Такой объект специально сделан асимметричным, чтобы на разных ракурсах было видно различие между front, back, left, right, top и bottom. На более позднем этапе в проект был добавлен отдельный тестовый инженерный объект test_part.obj, который используется для демонстрации работы baseline-методов на реальной плоской детали с отверстиями.

Папка `baselines/` содержит отдельные реализации baseline-методов. Сейчас в ней находятся пять файлов:

```text
iso.py
cap3d.py
algorithmic.py
isomap.py
vlm_iterative.py
```

Файл iso.py реализует ISO Orthographic Baseline: он создаёт шесть ортографических видов объекта и собирает их в коллаж по схеме first-angle или third-angle projection.

Файл cap3d.py реализует Cap3D-style Views Baseline: он создаёт восемь фиксированных перспективных ракурсов вокруг объекта и собирает их в сеточный multi-view collage.

Файл algorithmic.py реализует Algorithmic Top Views Baseline: он рендерит 24 кандидатных ракурса, рассчитывает визуальные признаки для каждого вида, выбирает восемь наиболее информативных и разнообразных ракурсов и собирает итоговый коллаж.

Файл isomap.py реализует Isomap View Selection Baseline: он рендерит 28 ракурсов объекта, извлекает image-based признаки, строит Isomap embedding, выбирает наборы из 3, 4 и 6 информативных и разнообразных видов и сохраняет несколько итоговых коллажей.

Файл `vlm_iterative.py` реализует VLM Iterative View Selection Baseline: он создаёт 28 candidate views, формирует первичный набор ракурсов, отправляет полный коллаж и текущий выбранный коллаж в мультимодальную модель, получает JSON-решение о замене видов и сохраняет финальный коллаж вместе с reasoning metadata.

Папка examples/models/ хранит тестовые 3D-модели, которые можно использовать для воспроизводимого запуска программы. Сейчас в ней находится модель test_part.obj.

Папка outputs/ хранит результаты работы baseline-методов. Для каждого baseline создаётся отдельная подписанная папка, внутри которой лежат отдельные запуски. Большинство запусков содержит два основных файла. Для baseline-методов, которые создают несколько визуальных результатов, папка запуска может содержать дополнительные изображения. Например, Isomap View Selection baseline сохраняет не только основной `collage.png`, но и `all_views_collage.png`, `collage_3.png`, `collage_4.png`, `collage_6.png` и `embedding.png`.

---

## Загрузка и нормализация модели

Перед запуском любого baseline программа должна привести 3D-модель к единому виду. Это необходимо, потому что разные модели могут иметь разный масштаб, положение в пространстве и количество объектов внутри сцены.

Сначала модель загружается из файла. Поддерживаемые форматы зависят от библиотеки `trimesh`, но обычно это:

```text
.obj
.stl
.ply
.glb
```

Если модель загружается как один mesh, программа использует его напрямую. Если модель загружается как сцена, состоящая из нескольких отдельных geometry-объектов, программа объединяет их в один общий mesh. Это нужно для того, чтобы baseline-методы могли работать с объектом единообразно.

После загрузки выполняется нормализация. Она состоит из двух шагов:

```text
1. Центрирование объекта
2. Масштабирование объекта
```

Центрирование переносит объект так, чтобы его bounding box оказался расположен вокруг начала координат. Это важно, потому что все камеры в программе смотрят в точку `(0, 0, 0)`. Если объект не центрировать, камера может смотреть мимо модели или модель будет находиться не в центре изображения.

Масштабирование приводит объект к единому размеру: максимальная сторона bounding box становится равной `1`. Это позволяет рендерить разные модели с одинаковыми параметрами камеры. Без этого маленькие модели могли бы быть почти невидимыми, а слишком большие — выходить за границы кадра.

---

## ISO Orthographic Baseline в программе

ISO baseline реализован в файле:

```text

src/baseline_lab/baselines/iso.py

```

Он создаёт шесть ортографических изображений объекта:

```text

front

back

right

left

top

bottom

```

Для каждого вида программа задаёт фиксированное положение камеры. Например, front-view получается камерой, расположенной перед объектом, back-view — камерой позади объекта, top-view — камерой сверху. Все камеры направлены в центр координат, то есть в центр нормализованного объекта.

Главное отличие ISO baseline от Cap3D-style baseline состоит в том, что здесь используется ортографическая камера. Ортографическая камера не создаёт перспективных искажений: удалённые и ближние части объекта не меняют размер в зависимости от расстояния до камеры. Благодаря этому изображение похоже на техническую проекцию.

После рендеринга шести видов программа собирает их в коллаж. Поддерживаются две схемы расположения:

```text

third-angle projection

first-angle projection

```

В third-angle projection виды располагаются интуитивно:

```text

        TOP

LEFT   FRONT   RIGHT   BACK

        BOTTOM

```

В first-angle projection расположение отличается:

```text

        BOTTOM

RIGHT  FRONT   LEFT    BACK

        TOP

```

На выходе ISO baseline возвращает изображение-коллаж и metadata-словарь, где фиксируются параметры запуска: название baseline, тип проекции, список видов и размер каждого изображения.

---

## Cap3D-style Views Baseline в программе

Cap3D-style baseline реализован в файле:

```text

src/baseline_lab/baselines/cap3d.py

```

Он генерирует восемь ракурсов объекта, равномерно расположенных вокруг вертикальной оси. В отличие от ISO baseline, здесь не используются front/top/side views в формате технического чертежа. Вместо этого камера движется вокруг объекта по окружности.

Текущая схема ракурсов выглядит так:

```text

0°

45°

90°

135°

180°

225°

270°

315°

```

Для каждого ракурса программа вычисляет положение камеры по углу азимута. Высота камеры задаётся параметром `elevation_deg`, а расстояние до объекта — параметром `radius`. По умолчанию используется восемь видов, но архитектурно этот параметр можно изменить.

Общий принцип работы такой:

```text

for each view:

    calculate camera position

    create render scene

    add mesh

    add perspective camera

    add light

    render image

```

В этом baseline используется перспективная камера. Это делает изображения более похожими на обычные визуальные рендеры, где объект воспринимается объёмно. Такой формат полезен для последующей работы VLM/LLM, потому что модель получает несколько визуально разных наблюдений объекта, а не только плоские инженерные проекции.

После рендеринга восьми изображений программа собирает их в коллаж в виде сетки. В текущей реализации используется раскладка `4 × 2`: четыре изображения в верхнем ряду и четыре изображения в нижнем. Каждый tile получает подпись с номером вида и углом камеры, например:

```text

VIEW_00_0DEG

VIEW_01_45DEG

VIEW_02_90DEG

...

VIEW_07_315DEG

```

На выходе Cap3D-style baseline сохраняет коллаж и metadata, где указаны количество ракурсов, угол elevation, радиус камеры, размер изображений и список всех сгенерированных views.

---

## Algorithmic Top Views Baseline в программе

Algorithmic Top Views baseline реализован в файле:

```text
src/baseline_lab/baselines/algorithmic.py
```

Этот baseline отличается от ISO и Cap3D-style подходов тем, что он не использует заранее фиксированный итоговый набор видов. Вместо этого программа сначала создаёт расширенный набор кандидатных ракурсов, затем оценивает каждый из них по визуальным признакам и выбирает наиболее информативные изображения для итогового коллажа.

В текущей реализации создаётся 24 кандидатных вида:

```text
8 azimuth angles × 3 elevation levels = 24 candidate views
```

Используются следующие уровни высоты камеры:

```text
15°
35°
60°
```

И восемь направлений вокруг объекта:

```text
0°
45°
90°
135°
180°
225°
270°
315°
```

Таким образом, программа получает набор ракурсов, который включает более низкие, средние и более верхние перспективные виды. Это особенно полезно для плоских инженерных деталей: низкие боковые ракурсы часто показывают только толщину объекта, а более высокие виды лучше раскрывают отверстия, силуэт и общую форму.

Общий принцип работы baseline:

```text
for each candidate view:
    calculate camera position
    render image
    create object mask
    calculate visual scores

select top 8 diverse views
assemble collage
save collage + metadata
```

Для оценки каждого вида программа строит простую маску объекта. Поскольку фон рендера белый, объект отделяется от фона через threshold: пиксели, которые заметно темнее белого, считаются частью объекта. На основе этой маски рассчитываются несколько признаков.

Используемые признаки:

```text
1. area_ratio — доля изображения, занятая объектом;
2. area_score — нормализованная оценка видимой площади объекта;
3. bbox_fill_score — насколько плотно объект заполняет свой 2D bounding box;
4. edge_ratio — доля контурных переходов в изображении;
5. edge_score — нормализованная оценка сложности силуэта и контуров;
6. base_score — итоговая визуальная оценка ракурса.
```

Итоговый score считается как взвешенная сумма:

```text
base_score =
    0.45 * area_score
  + 0.35 * edge_score
  + 0.20 * bbox_fill_score
```

После расчёта `base_score` программа выбирает восемь итоговых ракурсов. Выбор происходит не просто по top-8 максимальных оценок, а с учётом diversity. Это нужно, чтобы итоговый коллаж не состоял из почти одинаковых изображений. Первый вид выбирается по максимальному `base_score`, а следующие получают дополнительный бонус за угловое отличие от уже выбранных ракурсов.

Итоговый коллаж содержит восемь выбранных изображений. Каждый tile подписывается параметрами камеры и score:

```text
AZ000 EL35 S0.28
AZ180 EL35 S0.26
AZ270 EL35 S0.27
...
```

Где:

```text
AZ — azimuth angle;
EL — elevation angle;
S — base score выбранного вида.
```

На тестовой детали baseline выбрал преимущественно ракурсы с elevation 35° и 60°. Это показывает, что алгоритм отсекает почти боковые виды, где объект выглядит как тонкая полоска, и выбирает изображения, на которых лучше видны отверстия, толщина, силуэт и общая форма детали.

---

## Isomap View Selection Baseline в программе

Isomap View Selection baseline реализован в файле:

```text
src/baseline_lab/baselines/isomap.py
```

Этот baseline реализует полный multi-view pipeline: сначала программа формирует расширенный набор rendered views, затем извлекает признаки изображений, строит Isomap embedding и выбирает несколько наборов информативных ракурсов.

В текущей реализации создаётся 28 видов:

```text
Elevation 15°: 8 azimuth views
Elevation 35°: 8 azimuth views
Elevation 60°: 8 azimuth views
Elevation 80°: 4 high-angle views

Total: 28 views
```

Каждый rendered view описывается через feature vector. В него входят уменьшенное grayscale-изображение и несколько числовых признаков:

```text
1. area_ratio — доля изображения, занятая объектом;
2. bbox_fill_score — насколько плотно объект заполняет свой 2D bounding box;
3. edge_ratio — доля контурных переходов;
4. azimuth_deg — угол камеры вокруг объекта;
5. elevation_deg — высота камеры.
```

После извлечения признаков они стандартизируются и передаются в Isomap. Isomap строит двумерное пространство ракурсов, где похожие изображения оказываются ближе друг к другу, а различающиеся — дальше. Это пространство сохраняется в metadata и дополнительно визуализируется в файле `embedding.png`.

Общий принцип работы baseline:

```text
render 28 views
extract features
standardize features
build 2D Isomap embedding
calculate visual quality score
filter weak views by quality threshold
select diverse views in embedding space
assemble selected collages
save images + metadata
```

Для отбора видов используется quality-aware farthest point sampling. Сначала программа отбрасывает слишком слабые изображения с `quality_score < 0.45`. Затем первый вид выбирается по максимальному visual quality, а следующие виды выбираются с учётом двух факторов: удалённости от уже выбранных видов в Isomap embedding и собственной визуальной информативности.

Итоговая формула selection:

```text
selection_score =
    0.60 * isomap_diversity
  + 0.40 * visual_quality
```

На выходе baseline сохраняет:

```text
collage.png           — основной коллаж, совпадает с collage_6.png
all_views_collage.png — все 28 rendered views
collage_3.png         — 3 выбранных вида
collage_4.png         — 4 выбранных вида
collage_6.png         — 6 выбранных видов
embedding.png         — визуализация Isomap embedding
metadata.json         — параметры запуска, признаки, embedding и выбранные views
```

На тестовой детали финальный Isomap baseline выбрал в `collage_6` следующие ракурсы:

```text
AZ000 EL60
AZ315 EL60
AZ090 EL80
AZ225 EL60
AZ180 EL60
AZ000 EL35
```

Такой набор показывает, что метод выбрал преимущественно верхне-диагональные и почти верхние виды, где хорошо видны отверстия, общая форма и толщина детали. Слабые боковые ракурсы с elevation 15° были отфильтрованы по quality threshold.

---

## VLM Iterative View Selection Baseline в программе

VLM Iterative View Selection baseline реализован в файле:

```text

src/baseline_lab/baselines/vlm_iterative.py

```

Этот baseline использует мультимодальную модель для итеративного выбора наиболее информативных ракурсов. В текущей реализации используется Qwen vision-language model через OpenAI-compatible API.

Метод использует уже реализованную схему генерации 28 candidate views из Isomap baseline. После рендеринга для каждого вида рассчитываются image-based признаки и visual quality score. Затем программа создаёт начальный набор из шести видов с наибольшими quality score.

Общий принцип работы baseline:

```text

render 28 candidate views

calculate visual quality scores

select initial top-6 views

create all_views_collage

create iteration_0_collage

send all views + current collage to VLM

receive JSON decision

update selected views

repeat for 2 iterations

save final collage + metadata

```

Для обращения к мультимодальной модели используется OpenAI-compatible клиент. Параметры провайдера задаются через `.env`:

```env

QWEN_API_KEY=your_qwen_api_key_here

QWEN_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1

QWEN_VLM_MODEL=qwen-vl-plus

```

На каждой итерации в prompt передаются:

```text

1. полный коллаж из 28 candidate views;

2. текущий выбранный коллаж;

3. список текущих selected view indices;

4. metadata всех candidate views;

5. инструкция выбрать ровно 6 наиболее информативных ракурсов.

```

Модель должна вернуть JSON-ответ со следующими полями:

```json

{

  "previous_selected_view_indices": [16, 23, 17, 22, 8, 18],

  "selected_view_indices": [16, 23, 17, 22, 8, 20],

  "removed_view_indices": [18],

  "added_view_indices": [20],

  "reasoning": "The updated set provides a more informative and diverse view selection.",

  "visible_features": ["overall shape", "holes", "silhouette", "thickness"],

  "missing_or_weak_features": [],

  "confidence": 0.95

}

```

На выходе baseline сохраняет:

```text

collage.png             — основной финальный коллаж

all_views_collage.png   — все 28 candidate views

iteration_0_collage.png — начальный top-quality набор

iteration_1_collage.png — результат после первой VLM-итерации

iteration_2_collage.png — результат после второй VLM-итерации

final_collage.png       — финальный выбранный набор

metadata.json           — параметры запуска, reasoning и все решения модели

```

На тестовой детали начальный набор был:

```text

16 — AZ000 EL60

23 — AZ315 EL60

17 — AZ045 EL60

22 — AZ270 EL60

08 — AZ000 EL35

18 — AZ090 EL60

```

После первой итерации модель предложила заменить view `18` на view `20`:

```text

removed: 18 — AZ090 EL60

added:   20 — AZ180 EL60

```

Финальный набор:

```text

16 — AZ000 EL60

23 — AZ315 EL60

17 — AZ045 EL60

22 — AZ270 EL60

08 — AZ000 EL35

20 — AZ180 EL60

```

На второй итерации модель оставила набор без изменений. Это означает, что VLM оценила текущий коллаж как достаточно информативный и сбалансированный.

В metadata сохраняется не только финальный набор selected views, но и полный `iterations_log`. Это позволяет анализировать не только результат, но и сам процесс выбора: какие виды модель удалила, какие добавила и как объяснила своё решение.

---

## CLI: запуск программы из терминала

Программа запускается через команду:

```bash

baseline-lab

```

Основные параметры:

```text

--demo              использовать встроенный demo-объект

--input             путь к пользовательской 3D-модели

--baseline          выбор baseline: iso, cap3d, algorithmic, isomap или vlm_iterative

--projection        тип ISO-проекции: first или third

--image-size        размер каждого изображения в пикселях

--output-root       корневая папка для сохранения результатов

--run-name          название конкретного запуска

--out               прямой путь для сохранения результата

```

Пример запуска ISO baseline:

```bash
baseline-lab --demo --baseline iso --projection third --image-size 384
```

Пример запуска Cap3D-style baseline:

```bash
baseline-lab --demo --baseline cap3d --image-size 384 --run-name demo_cap3d_views
```

Пример запуска Algorithmic Top Views baseline:

```bash
baseline-lab --input examples/models/test_part.obj --baseline algorithmic --image-size 384 --run-name test_part_algorithmic_top8
```

Пример запуска алгоритмического baseline на своей модели:

```bash
baseline-lab --input path/to/model.obj --baseline algorithmic --image-size 384 --run-name my_model_algorithmic_top8
```

Пример запуска Isomap View Selection baseline:

```bash
baseline-lab --input examples/models/test_part.obj --baseline isomap --image-size 256 --run-name test_part_isomap
```

Пример запуска Isomap View Selection baseline на своей модели:

```bash
baseline-lab --input path/to/model.obj --baseline isomap --image-size 256 --run-name my_model_isomap
```

Пример запуска VLM Iterative View Selection baseline:

```bash
baseline-lab --input examples/models/test_part.obj --baseline vlm_iterative --image-size 256 --run-name test_part_vlm_iterative
```

Пример запуска на своей модели:

```bash
baseline-lab --input path/to/model.obj --baseline iso --projection third --image-size 384 --run-name my_model_iso
```

---

## Организация выходных данных

Для удобного просмотра результатов на GitHub программа сохраняет выходные данные каждого baseline в отдельную подписанную папку.

Общая структура сохранения:

```text

outputs/

    baseline_name/

        run_name/

            collage.png

            metadata.json

```

Например, результат ISO baseline сохраняется так:

```text

outputs/

    iso/

        demo_iso_third/

            collage.png

            metadata.json

```

А результат Cap3D-style baseline сохраняется так:

```text

outputs/

    cap3d/

        demo_cap3d_views/

            collage.png

            metadata.json

```

Результат Algorithmic Top Views baseline сохраняется так:

```text
outputs/
    algorithmic/
        test_part_algorithmic_top8/
            collage.png
            metadata.json
```

В папке `algorithmic` лежат результаты baseline, который автоматически выбирает наиболее информативные ракурсы из расширенного набора кандидатных видов.

Результат Isomap View Selection baseline сохраняется так:

```text
outputs/
    isomap/
        test_part_isomap/
            collage.png
            all_views_collage.png
            collage_3.png
            collage_4.png
            collage_6.png
            embedding.png
            metadata.json
```

В папке `isomap` лежат результаты baseline, который сначала создаёт 28 rendered views, затем строит Isomap embedding и выбирает несколько наборов информативных и разнообразных ракурсов.

Результат VLM Iterative View Selection baseline сохраняется так:

```text
outputs/
    vlm_iterative/
        test_part_vlm_iterative/
            collage.png
            all_views_collage.png
            iteration_0_collage.png
            iteration_1_collage.png
            iteration_2_collage.png
            final_collage.png
            metadata.json
```

В папке vlm_iterative лежат результаты baseline, который использует мультимодальную модель для итеративного выбора ракурсов. В отличие от остальных методов, он сохраняет не только финальный коллаж, но и промежуточные коллажи после каждой итерации.

Такой формат удобен для сравнения методов: можно открыть папку каждого baseline и сразу увидеть визуальный результат и параметры запуска. Это также упрощает публикацию результатов в GitHub-репозитории, потому что изображения и metadata-файлы лежат рядом и подписаны понятными именами.

---

## Metadata-файл

После каждого запуска программа сохраняет не только изображение, но и файл `metadata.json`. Он нужен для воспроизводимости эксперимента. В нём фиксируется, какой объект был использован, какой baseline был запущен, сколько времени занял запуск и с какими параметрами был получен результат.

Пример metadata для ISO baseline:

```json

{

  "input": "demo",

  "output": "outputs/iso/demo_iso_third/collage.png",

  "runtime_seconds": 0.327,

  "mesh": {

    "vertices": 24,

    "faces": 36,

    "extents": [1.0, 0.7, 0.8]

  },

  "result": {

    "baseline": "iso_orthographic",

    "projection": "third",

    "views": ["front", "back", "right", "left", "top", "bottom"],

    "image_size": 384

  }

}

```

Пример metadata для Cap3D-style baseline:

```json

{

  "input": "demo",

  "output": "outputs/cap3d/demo_cap3d_views/collage.png",

  "runtime_seconds": 0.42,

  "mesh": {

    "vertices": 24,

    "faces": 36,

    "extents": [1.0, 0.7, 0.8]

  },

  "result": {

    "baseline": "cap3d_views",

    "description": "Cap3D-style multi-view rendering baseline without captioning",

    "num_views": 8,

    "elevation_deg": 25.0,

    "radius": 2.5,

    "image_size": 384,

    "views": [

      "view_00_0deg",

      "view_01_45deg",

      "view_02_90deg",

      "view_03_135deg",

      "view_04_180deg",

      "view_05_225deg",

      "view_06_270deg",

      "view_07_315deg"

    ]

  }

}

```

Пример metadata для Algorithmic Top Views baseline:

```json
{
  "input": "examples/models/test_part.obj",
  "output": "outputs/algorithmic/test_part_algorithmic_top8/collage.png",
  "runtime_seconds": 0.643,
  "mesh": {
    "vertices": 9232,
    "faces": 16260,
    "extents": [1.0, 0.912, 0.084]
  },
  "result": {
    "baseline": "algorithmic_top_views",
    "description": "Algorithmic view selection based on silhouette area, bounding box fill, contour complexity, and view diversity",
    "candidate_views": 24,
    "selected_views": 8,
    "top_k": 8,
    "candidate_azimuths": 8,
    "candidate_elevations": [15.0, 35.0, 60.0],
    "radius": 2.5,
    "image_size": 384,
    "scoring": {
      "area_score": "normalized visible object area",
      "bbox_fill_score": "object mask area divided by its 2D bounding box area",
      "edge_score": "normalized silhouette/contour complexity",
      "final_score": "0.45 * area_score + 0.35 * edge_score + 0.20 * bbox_fill_score",
      "diversity": "greedy selection with angular diversity bonus"
    }
  }
}
```

Для algorithmic baseline metadata особенно важен, потому что он сохраняет не только итоговые параметры запуска, но и оценки всех кандидатных ракурсов. В поле `selected` записываются восемь выбранных видов, их rank, azimuth, elevation и score. В поле `all_candidates` сохраняются оценки всех 24 кандидатных изображений. Это позволяет проверить, почему алгоритм выбрал именно эти ракурсы, и делает метод интерпретируемым.

Пример metadata для Isomap View Selection baseline:

```json
{
  "input": "examples/models/test_part.obj",
  "output": "outputs/isomap/test_part_isomap/collage.png",
  "runtime_seconds": 0.652,
  "mesh": {
    "vertices": 9232,
    "faces": 16260,
    "extents": [1.0, 0.912, 0.084]
  },
  "result": {
    "baseline": "isomap_view_selection",
    "description": "28-view rendering pipeline with Isomap-based view selection",
    "total_views": 28,
    "selected_counts": [3, 4, 6],
    "image_size": 256,
    "radius": 2.5,
    "view_generation": {
      "elevation_15": "8 azimuth views",
      "elevation_35": "8 azimuth views",
      "elevation_60": "8 azimuth views",
      "elevation_80": "4 high-angle views",
      "total": "28 views"
    },
    "selection": {
      "method": "quality-aware farthest point sampling in 2D Isomap embedding",
      "quality_threshold": 0.45,
      "selection_score": "0.60 * isomap_diversity + 0.40 * visual_quality",
      "outputs": ["collage_3.png", "collage_4.png", "collage_6.png"]
    }
  }
}
```

Для Isomap View Selection baseline metadata сохраняет не только параметры запуска, но и полный список всех 28 rendered views. Для каждого вида фиксируются azimuth, elevation, image-based признаки, quality score и координаты в Isomap embedding. В поле `selected` отдельно сохраняются выбранные наборы для `collage_3.png`, `collage_4.png` и `collage_6.png`. Это позволяет проверить, какие ракурсы были выбраны и почему.

---

## 2.9. В `Metadata-файл` добавить VLM metadata

После Isomap metadata добавь:

````md
Пример metadata для VLM Iterative View Selection baseline:

```json
{
  "input": "examples/models/test_part.obj",
  "output": "outputs/vlm_iterative/test_part_vlm_iterative/collage.png",
  "runtime_seconds": 9.302,
  "mesh": {
    "vertices": 9232,
    "faces": 16260,
    "extents": [1.0, 0.912, 0.084]
  },
  "result": {
    "baseline": "vlm_iterative_view_selection",
    "model": "qwen-vl-plus",
    "candidate_views": 28,
    "target_views": 6,
    "iterations": 2,
    "initial_selection": {
      "method": "top visual quality scores from 28 candidates",
      "selected_view_indices": [16, 23, 17, 22, 8, 18]
    },
    "final_selection": {
      "selected_view_indices": [16, 23, 17, 22, 8, 20]
    },
    "iterations_log": [
      {
        "iteration": 1,
        "selected_view_indices": [16, 23, 17, 22, 8, 20],
        "vlm_decision": {
          "removed_view_indices": [18],
          "added_view_indices": [20],
          "reasoning": "The updated set provides a more informative and diverse view selection.",
          "confidence": 0.95
        }
      }
    ]
  }
}
```
````
Для VLM Iterative baseline metadata особенно важен, потому что он сохраняет reasoning мультимодальной модели. В поле `iterations_log` фиксируются решения каждой итерации: исходный набор видов, новый выбранный набор, удалённые и добавленные views, объяснение модели и confidence. Благодаря этому результат можно анализировать и сравнивать с более интерпретируемыми algorithmic и Isomap baseline.

Metadata особенно важен для будущего режима сравнения baseline-методов. Когда появится несколько способов генерации коллажей, можно будет сравнивать не только картинки, но и параметры их создания: количество видов, тип камеры, время выполнения, размер изображений и другие характеристики.

---